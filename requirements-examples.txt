# Requirements for running all 8 example cases locally.
# Pin versions for reproducibility.
#
# Install:
#   pip install -r requirements-examples.txt
#
# Or use the package extras directly (unpinned):
#   pip install het-ai[examples,dvc,mlflow]

# ── het-ai core ──────────────────────────────────────────────────────────────
optuna>=3.5.0
numpy>=1.24.0
pandas>=2.0.0

# ── DVC + storage ─────────────────────────────────────────────────────────────
dvc>=3.0.0
minio>=7.0.0
requests>=2.28.0

# ── MLflow ────────────────────────────────────────────────────────────────────
mlflow>=2.10.0

# ── ML frameworks ─────────────────────────────────────────────────────────────
torch>=2.1.0
onnx>=1.15.0
onnxscript>=0.1.0
onnxruntime>=1.16.0
tensorflow>=2.14.0
Pillow>=10.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
pymc>=5.9.0
arviz>=0.17.0
netCDF4>=1.6.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
